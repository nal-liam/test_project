const names = require('./names');
const created_function = require('./functions');
const greetUser = require('./functions');
//defining the function to be used from another module 
greetUser(names.first_user);

